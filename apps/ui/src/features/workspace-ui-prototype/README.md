# PROTOTYPE — wipe me

Three variants of the Workspace coding-agent chrome, switchable via `?variant=` on `/.workspace-ui-prototype`.

Scenes (`?scene=`): `populated` · `empty-org` · `empty-ws` · `readonly`.

This is throwaway evidence for [Workspace UI prototype](../../../../.ai/scratchpad/git-backed-projects/issues/15-project-workspace-ui-prototype.md). Do not promote these files to production routes.
