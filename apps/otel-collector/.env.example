# Contributor collector (Compose profile `infra`). No tokens.
# The process prints OTLP to its own logs. Hosted ingest is
# https://telemetry.ctxpipe.ai (ADR-038), configured on the app via
# OTEL_EXPORTER_OTLP_*, not on this collector.
