/**
 * Better Auth configuration. Wire up providers, database adapter, and session
 * handling here. Integrate with Hono via middleware that attaches the
 * authenticated user to the context.
 *
 * @see https://www.better-auth.com/
 */
// Placeholder: add betterAuth({ ... }) and export for use in app middleware
export {}
