---
"@ctxpipe/aws-cdk": patch
---

Let self-hosters register a public Notion integration in the product UI so empty `NOTION_*` env still connects and receives signed webhooks. Hosted keeps the env-owned one-click path.
