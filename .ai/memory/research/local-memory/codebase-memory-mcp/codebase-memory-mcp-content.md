# Codebase-Memory MCP Content Model

Sources: https://github.com/cranbis/codebase-memory

## What It Stores

Public search results describe Codebase-Memory MCP as storing persistent codebase knowledge, including:

- codebase structure;
- decisions;
- dependencies;
- implementation patterns;
- repository-aware context.

Exact storage schema was not visible in accessible search results.

## Semantics / Types It Looks For

Likely semantics:

- architecture facts;
- code organization;
- module dependencies;
- implementation decisions;
- reusable patterns;
- project-specific conventions.

## Extraction Prompt

No extraction prompt was found. The public repo needs direct inspection for tool schemas and prompts.

## How It Manages Memory Soup

Unknown from public sources. The concept itself narrows scope to codebase memory, which helps avoid generic personal-memory soup, but there is no visible evidence of:

- typed records;
- deduplication;
- decay;
- branch scoping;
- provenance;
- memory review.

## Notes For ctxpipe

The product idea is relevant, but the project did not expose enough detail to learn from its content model without a code audit.

