# ctx| logo iteration journal

Brand teal: `#40E0D0` on dark, `#0F766E` on light.
Protect: lowercase `ctx`, teal reserved for the route, terminal pipe, dark-first, mark ≠ wordmark.
Kill: pixel nostalgia, second pipe on the wordmark, hamburger/filter read, wordmark-as-icon.

## Loop 0 — current shipped `public/ctx_.svg`

Sol: **replace**. Pixel mark is illegible at 16–20px, gamey, no icon crop, white-only asset fails on light/merch. Split mark from wordmark.

## Loop 1 — four foundations

| ID | Name | Sol rank |
| --- | --- | --- |
| A | Routed Context | **1 — winner** |
| B | Precision ctx| | 2 |
| C | Context Capsule | 4 |
| D | Cut-Pipe Monogram | 3 |

Winner A. Kill B notch, C square badge, D fused cx|. Do not put `|` on the wordmark.

## Loop 2 brief (variations of A)

1. Hard Dock — bars physically join the pipe; longer stdout
2. Pipe First — heavier/taller teal pipe; shorter staggered inlets
3. Typographic Route — redraw lowercase ctx; shared geometry with mark
4. Asymmetric Route — long middle inlet, offset stdout
