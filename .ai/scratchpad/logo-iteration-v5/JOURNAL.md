# Pass 5 — terminal mark tournament

Echo wordmark (pass 4) stays in `mix/` and in `winner/alternate-echo-*`. This pass inhabits a **terminal object**.

Calibration: known-broken fixture RED.

Concept review after loops 1, 2, and 4 (separate agent, first-read only, no “would you ship?”).

| Loop | Basis | Four descendants | Winner | Reviewer notes carried |
| --- | --- | --- | --- | --- |
| 1 | empty terminal | chrome · cli · bezel · pipe-frame | **A chrome** | Title bar is the object. Ignore `>`, fat right pipe, traffic lights. Thin bar, hairline, even inset. |
| 2 | L1A | thin · echo-glass · tab · **knockout** | **D knockout** | Glass becomes a body. Echo stays the other family. |
| 3 | L2D | **craft knock** · raised · echo-cut · deep | **A craft** | Loop was too close (four slabs). Carry title/frame craft; next loop must change the object. |
| 4 | L3A | square · name-in-chrome · **slab** · wide | **C slab** | Two-material window, no hairline. Do not square or widen. Zinc must stay a slab at 20px. |
| 5 | L4C | **craft slab** · echo-cut · tick · raised | **A craft slab** | Thicker zinc, more glass above `c`. |
| 6 | L5A | **color** · mono · invert · night | **A color** | Teal glass is the brand. |
| 7 | L6A | ref · more glass · **heavier zinc** · tighter | **C heavier zinc** | Metal strip as a real slab. |

**Lineage:** empty terminal → chrome → knockout → craft knock → slab → craft slab → color → heavier zinc.

**Recommended (terminal family):** zinc title slab + teal glass with pixel `ctx|` knocked out. Sharp. Dark `#40E0D0` / light `#0F766E`.

**Still in the mix:** pass 4 echo wordmark (`mix/echo-*.svg`).

Production `apps/ui/public/ctx_.svg` is unchanged.
