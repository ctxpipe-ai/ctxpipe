# Brief — ctx| identity, pass 5 (terminal mark)

The echo wordmark (pass 4) stays in the mix. This pass is a **different architecture**: a terminal icon with `ctx|` or `ctxpipe` built into it. Not another styled string.

- **Name / pitch:** ctxpipe — self-learning context layer for autonomous AI agent fleets. Engineers. Terminal-native.
- **Job:** a **mark** you can play with (app icon, merch, deck) that still says ctxpipe. The object is a terminal; the name lives in the chrome or on the glass.
- **Surfaces:** dark UI first; light; 20px sidenav (letters must still read — frame the word, do not shrink it into a 16px screen); 16px favicon (hinted window); merch / one-color.
- **Color:** teal `#40E0D0` on dark; `#0F766E` on light. Ink `#F4F4F5` / `#09090B`. Sharp (`rounded-none`).
- **Protect:** shipped pixel `ctx` when letters appear; reading `ctx|`; teal as accent; the echo winner as an alternate (do not delete it).
- **Do not:** homemade `ctx` bars; macOS traffic-light cliché unless a loop earns it; the pass-1 L-diagram; a window that hides the letters at 20px.
- **Process:** loop 1 basis = a plain terminal. Four ways to put `ctx|` / `ctxpipe` into that object. Winner is the only basis for the next four. Concept review on green work only; at most three craft notes per loop.

## Architectures in play (loop 1)

Chrome (title + screen) · CLI frame (`>` + ctx|) · bezel / glass · pipe-as-frame. One idea per candidate.
