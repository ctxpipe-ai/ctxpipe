# Loop 1 scorecards

Calibration this session: fixture RED (known-broken L-diagram). Reviewer: implementing agent.

## A solid register — GREEN

| ID | Verdict | Evidence |
| --- | --- | --- |
| TYPE | green | Same shipped pixel `ctx` as the product face; holds next to Geist as a system, not traces |
| CONSTRUCT | green | One letter system; echo is a second hit of the same paths |
| LIGHT | green | Black letters + dark teal echo on white |
| NAV20 | green | `ctx` still names at 20px; echo reads as a rim |
| SQUINT | green | One word + teal pipe |
| GLYPH | green | Reads `ctx|` with a print offset |
| UNIT | green | Letters and pipe share the echo |
| INK | green | Two offset bars still a mark |
| DECK | green | Refined echo, deck-ready |

## B trail — GREEN (busy)

Same letters. Three fades survive light/dark. At 20px the trail starts to glow; still names `ctx`. More motion than we need if A is the refine.

## C east register — GREEN (quiet)

Same letters. X-only offset is a real idea; at first-read it can look like a thin highlight, less brand than A.

## D knockout — RED on UNIT / GLYPH

Teal plate + separate pipe. Letters become holes; the ghost sits in the cut and fights the plate. Badge, not a lockup.

**Winner: A solid register.** Basis for loop 2.
