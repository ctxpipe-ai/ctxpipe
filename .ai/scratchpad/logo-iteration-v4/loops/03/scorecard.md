# Loop 3 scorecards

Basis: L2B double hit.

## A split axis — GREEN — winner

East hit + south hit. Same pixel letters. Reads as print misregister, not a second diagram. Two-direction echo is a real idea.

## B pipe afterimage — RED on GLYPH / UNIT

Horizontal teal bars off the pipe collide with `x` and first-read as a trail/signal, not one lockup.

## C plate — GREEN (weak)

Zinc card is barely a card on dark (`#18181B` on `#09090B`). Extra object without extra meaning.

## D invert double — GREEN

Teal letters are a color flip. Distinct, but it spends the accent on the word instead of the echo.

**Winner: A split axis.**
