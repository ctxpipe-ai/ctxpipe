# Recommended family (pass 4, echo tournament, loop 10A)

Shipped pixel `ctx` + three-step teal east echo + fat teal pipe.

| File | Use |
| --- | --- |
| `lockup-dark.svg` / `lockup-light.svg` | Product / light |
| `mark-64.svg` / `mark-64-mono.svg` | App icon (three offset bars) |
| `mark-16.svg` | Favicon |
| `alternate-mono-dark.svg` | Merch / one-color (loop 9B) |

Lineage: `.ai/scratchpad/logo-iteration-v4/JOURNAL.md`.

Production `apps/ui/public/ctx_.svg` is unchanged.
