# Pass 4 — echo tournament

Basis 0 = the liked echo from pass 3 loop 1D (shipped pixel `ctx` + teal offset). Each loop: **four new descendants**, pick one, that pick is the **only** basis for the next loop. Four-up sheets show the basis in the teal strip.

| Loop | Basis | Four descendants | Winner | Why |
| --- | --- | --- | --- | --- |
| 1 | crude echo | solid register · trail · east register · knockout | **A solid register** | Same echo, no 0.85 mush, pipe echoed, canvas open. Knockout RED (plate + pipe). |
| 2 | L1A | south drop · **double hit** · NE · fat pipe | **B double hit** | Two solid SE hits. Louder echo. (This loop was too close — direction/weight only.) |
| 3 | L2B | **split axis** · pipe afterimage · plate · invert | **A split axis** | East + south register. Pipe afterimage RED (`ctx` + signal bars). |
| 4 | L3A | unequal · **clean pipe** · scan · tall pipe | **B clean pipe** | Dual register stays on the letters; pipe is one accent. |
| 5 | L4B | **heavy east** · x-only · south-heavy · stacked bar | **A heavy east** | 3c east toward the pipe. Stacked bar RED (underline, lost `ctx\|`). |
| 6 | L5A | east only · pipe joins · **stepped east** · dock | **C stepped east** | Two solid east hits. Pipe joins RED (`ctx\|\|`). |
| 7 | L6C | **three step** · step+south · fat pipe · teal word | **A three step** | Echo is now a register trail. Fat pipe kept as a sibling. |
| 8 | L7A | tighter · **three + fat** · no pipe · tall pipe | **B three + fat** | Absorbs the fat-pipe sibling. No pipe RED. |
| 9 | L8B | **color** · mono · invert · night | **A color** | Teal trail is the brand; mono is merch. (Color system, not a new architecture.) |
| 10 | L9A | **ref** · dock · two-step · tighter | **A ref** | Assembled family. |

**Lineage (winner chain):** crude echo → solid register → double hit → split axis → clean pipe → heavy east → stepped east → three step → three+fat → color → ref.

**Recommended:** shipped pixel `ctx` + three-step teal east echo + fat teal pipe. Dark `#40E0D0` / light `#0F766E`. Mark = three offset bars. 16px = two bars.

Production `apps/ui/public/ctx_.svg` is unchanged.
