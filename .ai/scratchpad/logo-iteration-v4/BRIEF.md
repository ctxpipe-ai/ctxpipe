# Brief — ctx| identity, pass 4 (echo tournament)

Pass 3 explored well but **broke the tournament**: later loops reset to independent ideas (invert, block, chamfer) and optical one-offs. The human liked **echo** and wants it refined, plus more ideas that still come from the last winner.

- **Name / pitch:** ctxpipe — self-learning context layer for autonomous AI agent fleets. Engineers. Terminal-native.
- **Job:** a brand with personality. Echo is the idea to refine (print misregister / afterimage / delayed cursor).
- **Surfaces:** dark UI first; light; 20px sidenav; 16px favicon; merch / one-color.
- **Color:** teal `#40E0D0` on dark; `#0F766E` on light. Ink `#F4F4F5` / `#09090B`.
- **Protect:** shipped pixel `ctx` letters; reading `ctx|`; pixel grit; oversized pipe; **echo as the brand move**.
- **Do not protect:** the crude v3 echo (clipped canvas, 0.85 mush, pipe with no echo); pass-1 L-diagram; generic Geist-only wordmark.
- **Process:** loop 1 basis = the liked echo. Each loop draws **four new descendants** of that basis. Pick one. That pick is the only basis for the next loop. Show the basis on every four-up sheet.

## Architectures (all must still be echo)

Register (offset letters) · trail (stacked fades) · pipe afterimage · knockout / field · directional misregister. One idea per candidate. No abandoning echo for a new mark family.
