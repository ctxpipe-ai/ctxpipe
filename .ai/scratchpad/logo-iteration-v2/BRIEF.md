# Brief — ctx| identity, pass 2

- **Name:** ctxpipe, lockup `ctx|` (“context pipe”, Unix pipe).
- **Audience:** engineers and agent operators. Professional, modern, high-tech. Not playful, not a diagram of the product.
- **Job of the mark:** identify the product in 20px sidenav chrome, on a light marketing surface, and as a 16px tab icon.
- **Surfaces:** dark UI first; light UI / docs; merch / one-color; 20px sidenav (`SideNavLogo` `h-5`); 16px favicon.
- **Color:** teal accent `#40E0D0` on dark; darker teal on light (`#0F766E` / `#087F73` range). Ink `#F4F4F5` / `#09090B`. Teal is the pipe, not a coat of paint.
- **Protect from the current mark:** the reading `ctx|`; teal on the pipe only; sharp / product-native (Geist, `rounded-none`).
- **Do not protect:** the homemade pixel-grid drawing (white-only, gamey at 20px); the last-loop two-inlet / L diagram.

## Architectures this pass

1. **Wordmark** — `ctx|` in a real product face (Geist or Geist Pixel Square).
2. **Letterform-as-symbol** — the `|` as the square mark / favicon, derived from the same wordmark.
3. **Lockup** — only if a mark other than `|` survives the scorecard on its own. No second diagram beside the letters.

Start from the current set: shipped pixel `ctx_.svg`, onboarding Geist Pixel Square `ctx|`, product Geist.
