# Recommended family (pass 2, loop 10)

Geist Pixel Square `ctx|`. Teal on the pipe only. Same `|` glyph as the square mark.

| File | Use |
| --- | --- |
| `lockup-dark.svg` | Product chrome, dark |
| `lockup-light.svg` | Light / docs |
| `wordmark-dark.svg` | One-color wordmark |
| `mark-64.svg` / `-light` / `-mono` | App icon / avatar |
| `mark-16.svg` / `-mono` | Favicon (hinted 4×14) |
| `alternate-mono-dark.svg` | Green alternate (Geist Mono), not the pick |

Integrity: GREEN (see `../loops/10/scorecard.md`). Production `apps/ui/public/ctx_.svg` is unchanged.
