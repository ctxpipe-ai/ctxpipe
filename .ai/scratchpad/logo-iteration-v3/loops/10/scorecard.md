# Integrity scorecard

- Candidate: Loop 10A recommended — pixel `ctx` + teal echo + chamfered teal block
- Sheet: `renders/10-a.png` + compare + first-read
- Reviewer: implementing agent
- Calibration fixture this session: RED

| ID | Verdict | Evidence |
| --- | --- | --- |
| TYPE | green | Same pixel letter system as the shipped mark, not unrelated stacked bars |
| CONSTRUCT | green | One grid; `c`/`t`/`x` share weight |
| LIGHT | green | Ink letters + darker teal block on white |
| NAV20 | soft | Pixel `t`/`x` are coarse at 20px (same as shipped); still nameable |
| SQUINT | green | One word + a teal mass |
| GLYPH | green | Reads `ctx` + a cursor/block, not `+` / hamburger / equals |
| UNIT | green | Letters and block lock as one object |
| INK | green | Chamfer block still a mark in one ink |
| DECK | green | Has a rememberable hit (echo + cut block) |

**Result:** GREEN for this brief (personality-bearing pixel system). NAV20 is the known shipped weakness, not a new break.
