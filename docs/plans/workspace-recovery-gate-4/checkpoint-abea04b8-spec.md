# Gate 4 Spec review — `abea04b8`

**Findings: 0 blocking, 0 nonblocking.**

The implementation satisfies the prior design constraints and the accepted requirement that a warm turn perform “no sandbox-definition rebuild” (`docs/plans/workspace-chat-recovery.md:329-332`). The application creates one module-static definition for each implemented provider (`tanstack-workspace-chat.ts:112-157`) and passes a freshly captured workspace to both direct prepare ensure (`:253-294`) and stock `withSandbox` (`:322-403`). It adds no handle map, binding registry, or lease facade, consistent with the required removal at plan lines 294-300 and 673-674.

Per-operation identity is complete for this slice: `WorkspaceDefinition.identity` carries the full captured revision plus Docker image identity (`tanstack-workspace-chat.ts:496-517, 583-597`), while the native workspace hash excludes `secrets`. The package patch captures the effective workspace once and uses it for keying, resume secret reinjection, restore/create, bootstrap, `ensureExisting`, and destroy (`patches/@tanstack__ai-sandbox@0.5.0.patch:124-194, 429-511`). Middleware consistently uses that same captured workspace for checkpoint policy, restore/watch roots, projection, snapshot capture/redaction, and contextual `onReady` (`:23-97, 331-406`). Named snapshot creation also binds/stages the selected runtime workspace and passes it to native existing-handle lookup (`:224-280, 512-588`). The previous dynamic session-branch hook now receives immutable ensure context rather than closing over request state (`tanstack-workspace-chat.ts:122-143`).

Docker selection uses the native Docker environment probe and the definition’s image is digest-pinned; that digest is also in workspace identity (`chat-runtime.ts:20-29`, `tanstack-workspace-chat.ts:512-517`). Acknowledged warm GitHub/base-fork and Railway work remains outside this checkpoint verdict. Supplied 18 native checks and zero-new-diagnostic type results were accepted; no tests were rerun.
