# Gate 4 checkpoint: native bases and live revision recovery

Baseline: `40c6e8b0`. Gates 0–3 remain complete. This checkpoint advances Gate 4; provider/security work and final gate acceptance remain open.

Native sandbox definitions now create credential-free, captured-revision base snapshots and apply per-thread setup/secrets after forking. Compatible live threads keep their actual branch/worktree across SHA changes. Native exact-key ownership moves only after Git succeeds and the Workspace binding/SHA still matches in a short database transaction. Durable Git markers and retained stashes make interruption, conflict, and superseded-target recovery explicit. Pre-upgrade records without reliable compatibility metadata return a conflict identifying the retained owner instead of allocating over saved work.

Files and push operations hold the same native transcript lock as chat through their complete operation. Lease/request cancellation reaches native Git and filesystem operations. Temporary native patches add optional filesystem signals; Docker forwards them through each write chunk and waits for process termination. The local provider uses cancellable native filesystem/command paths. Files status and the repair agent receive effective/current target revisions; publishing stays blocked while stale.

Base cleanup retains shared images while any fork references them, and collects unused bases after revision/image changes or inactivity. Nullable transition/image metadata migrations are generated by Drizzle.

| Focused evidence | Result |
| --- | --- |
| Shared-base reuse, captured SHA, credential separation, last-owner image cleanup | Passed |
| App base/thread setup wiring | 13 tests passed |
| Live advance, rewind, stale queued request, and Workspace CAS race | Passed |
| Actual process loss after Git completion but before ownership move | Passed; same handle and saved edits recovered |
| Actual Docker container replacement after live revision update | Passed; new current-revision base restored |
| Published/default branch conflict and retained-edit recovery | Passed |
| Exact native store conformance, Files operations, push serialization, real lease-loss write prevention, blocked Docker FIFO cancellation | 13 tests passed, 37.12 seconds |
| Image rotation cleanup before any replacement base exists | Passed, 27.85 seconds; active worktree preserved |
| Current-target notices, queued stale turn, legacy-owner protection | 4 tests passed, 26.01 seconds |
| Frozen offline dependency install | Passed |
| Generated transition/image migrations: local, fresh database, and upgrade from baseline | Passed; existing fixture rows retained |
| Test policy and CI partition | Passed; 190 backend + 42 contracts, no overlap/missing files |
| Full-project backend/UI type baselines | Passed; 132/225 existing diagnostics, no additions or stale allowances |
| Focused UI publication-state tests and formatting | Passed |

Both final focused review axes report zero blocking findings; see checkpoint-native-bases-spec.md and checkpoint-native-bases-standards.md. The checkpoint full CI run follows this push; its result will be recorded with the next milestone. Detailed logs remain local and are not committed.
