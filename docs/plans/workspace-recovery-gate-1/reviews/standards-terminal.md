# Gate 1 terminal standards review

**Fixed point:** `7dfa6b93a5baedc3eb2c86dd1056662e89cace00`  
**Reviewed candidate:** `d87858354a783a9fd95c46785208c9b699a45e3b`  
**Code verdict:** **PASS — zero blocking findings**  
**Gate verdict:** **PASS — Gate 1 complete with zero blocking findings.**

Independent terminal verification confirms that `refs/heads/codex/develop-plan-to-refocus-branch-direction` resolves to the reviewed candidate. GitHub Actions run `34174216707` is `completed/success`, has that exact `head_sha`, and reports all 13 jobs `completed/success`. The retained raw metadata is `docs/plans/workspace-recovery-gate-1/logs/ci-d8785835-complete.json`.

The review covered the complete three-dot diff, repository-wide callers and parallel paths, Gate 0 classification, Gate 1 proof, root and affected-app `AGENTS.md`, `.agents/skills/tdd/{SKILL.md,tests.md,mocking.md}`, `.agents/skills/code-review/SKILL.md`, ADR-002, ADR-028, ADR-031, and the recovery plan.

## Requirement matrix

| Gate 1 requirement | Result | Implementation evidence | Execution/oracle evidence |
| --- | --- | --- | --- |
| 1. Full typecheck or finite diagnostic allowlist that only shrinks | **PASS** | `scripts/ci/typecheck-all.mjs:6-49` runs all seven entries in `scripts/ci/projects.json` and their generation/build prerequisites. `scripts/ci/typecheck.mjs:34-126` constructs the real TypeScript program, prints every diagnostic, keys allowances by file/code/message/count, rejects new/count-changed diagnostics, and rejects stale allowances. `scripts/ci/check-allowlist-history.mjs` prevents re-adding removed debt. | `logs/typecheck-workflow-contracts.log`, `logs/typecheck-all-lanes.log`, and `logs/allowlist-history-green.log`: backend 177, UI 391, codesearch 11, CDK/CLI/self-host/docs zero. Exact-candidate CI job `Typecheck` succeeded. |
| 2. Production builds for every affected runnable/package surface | **PASS** | `.github/workflows/ci.yaml:147-173` builds backend, worker, UI, codesearch, docs, and OTEL images. Lines 175-197 build backend/codesearch entrypoints, distributable CDK/CLI, and typecheck self-host. Lines 215-231 validate Terraform without its backend. Backend, worker, UI, docs, and codesearch Dockerfiles retain Bun production entrypoints; dependency/frontend stages use Node 22 per ADR-031. `.dockerignore:1-14` preserves required source/patch/lock inputs while excluding local outputs and evidence. | Exact-candidate CI run `34174216707` succeeded for all six production images, runnable packages/examples, codesearch real toolchain contracts, and Terraform. Local build/Terraform logs are also retained. |
| 3. Required prerequisites explicit; missing binary or skipped proof fails | **PASS** | `scripts/ci/prerequisites.mjs:5-89` requires Node 22, Git, Bun >=1.3.11, the pinned OpenCode version, auth, migrated PostgreSQL, and a nonsuperuser/nonowner/non-BYPASSRLS app role. `.github/workflows/ci.yaml:98-137` installs the declared tools, migrates, and invokes required runners. `scripts/ci/test-suite.mjs:27-62,93-101` fails on missing contract files, empty inventory, runner abnormality, or report failure. | `scripts/tests/ci-prerequisites.test.mjs` and retained prerequisite red/green logs prove missing tools fail. `scripts/ci/check-test-report.mjs:16-91` rejects empty, skipped/todo, abnormal, mismatched-suite, inventory, total, exit, unexpected-failure, duplicate, and stale-allowance results. |
| 4. Deterministic Postgres, git, local sandbox, hydrate, job-runner, and chat lanes | **PASS for Gate 1 scope** | `scripts/ci/contracts.json` names each required file; `test-suite.mjs:40-51` fails if any is absent. PostgreSQL/RLS: `src/db/rls-isolation.test.ts`, `src/models/workspaces-sandbox-persist.test.ts`. Native git: `src/services/git/clone-tree.test.ts`. Local sandbox/worktree: `src/domain/workspaces/job-sandbox.live.test.ts`. Hydration: `hydration.contract.test.ts:45-159` uses real Git, PostgreSQL OpenWorkflow, 0/1/100-file fixtures, persisted projection/SHA/embedding assertions, and cleanup. Job runner: `write-workflow.contract.test.ts:20-147` executes all 12 schema kinds through real OpenWorkflow/Postgres and verifies the exact current failure, no job row, and no remote-ref change. Chat: `tanstack-workspace-chat.multiturn.test.ts:230-424` runs two real OpenCode turns through PostgreSQL stores/local sandbox/Hono model proxy, reconstructs persisted history, and destroys the sandbox row. | `logs/contracts-full-workflows.log`: 23 cases in seven files, zero skipped. `logs/hydration-workflow-real.log`, `logs/write-workflow-all-refs.log`, and `logs/chat-reconstruction-cleanup.log` retain seam-specific proof. Write success remains expressly Gate 3 work; restart/replica chat invariants remain Gate 4 work. |
| 5. Reject skip/fails/todo, blind retries, and owned module mocks in proof | **PASS** | `scripts/ci/check-test-policy.mjs` parses all proof/config/runner sources using TypeScript symbols; resolves lexical aliases, tuple spreads, static/computed keys, wrapper calls, mutations, namespace test members, and Node/Bun/Vitest mocks. `scripts/ci/test-configuration.mjs:6-133` parses package scripts, YAML workflows, and shell, follows custom config flags and local ESM/re-export/CJS dependencies, and detects direct and forwarded retry flags. Gate 0 characterization comes from immutable commit `7dfa6b93…`, not an editable head file. | `scripts/tests/ci-test-policy.test.mjs` contains public red/green coverage for the reviewed bypass classes. Independent probes covered shorthand/scope/mutation/call-return/config/YAML/CJS/forwarding/namespace/table-data cases. `pnpm test:policy` reports **433 source/config/runner files and 27 command files**. Five `scripts/tests/ci-*.test.mjs` tests pass. |
| Exit: green proves stated modules executed; allowances cannot hide path filters or silent skips | **PASS** | Full source inventories replace path selection, structured reports couple suite/case/file totals to process exit, exact diagnostic/failure identities must remain present, and CI publishes `.ci-results`. ADR-031 matches the implementation and distinguishes characterization from proof. | Local evidence is green. Exact-candidate run `34174216707` completed successfully with all 13 jobs green, and the remote branch resolves to the same SHA. |

## Blocking findings

| Axis | Result | Evidence/limit |
| --- | --- | --- |
| Correctness | **None** | Re-reviewed every prior policy bypass at the final candidate; forwarding, namespace assertions, explicit zero, config graphs, mutable options, and call-return construction behave as intended. |
| Security | **None** | Contract prerequisites reject privileged/table-owning DB roles (`prerequisites.mjs:57-80`); production image contexts exclude secrets; no credential/push behavior changed in Gate 1. |
| Migration | **None** | CI applies normal, OpenWorkflow, and checkpoint migrations before contracts (`ci.yaml:112-126`) and separately exercises migration validation; Gate 1 adds no schema migration. |
| Performance | **None** | Gate 1 changes CI/proof rather than product hot paths. Production/codesearch jobs have bounded 30-minute limits; prior Linux stalls are retained rather than hidden. Product latency budgets remain later-gate acceptance work. |
| Test oracle | **None** | Required file inventories, structured totals/status/process exit, immutable characterization classification, exact failure identities, and no-skip policy are all enforced. |

No actionable Fowler-baseline smell remains in the Gate 1 implementation. Extracting `scripts/ci/test-configuration.mjs` gives command/config discovery one owner and removes the earlier possible Divergent Change in the AST policy module.

## Codebase coverage map

| Searched subsystem / entry point | Files and paths inspected | Completeness result |
| --- | --- | --- |
| CI entry points | `.github/workflows/ci.yaml`, `.github/workflows/cli-test.yaml`, every tracked `package.json`, `scripts/**/*.sh` | All build/test/typecheck/migrate/Terraform entry points are included by workflow jobs or the 27-command policy inventory. Filtered pnpm/npm/Turbo forwarding was probed directly. |
| Typecheck | `scripts/ci/{projects.json,typecheck-all.mjs,typecheck.mjs,check-allowlist-history.mjs}`, all seven `diagnostics/*.json`, legacy `scripts/typecheck-{ui-,}workspaces.sh` | Seven real compiler programs execute; legacy path-filter behavior was removed and searches found no alternate filtered CI owner. |
| Test execution/oracle | `scripts/ci/{test-suite.mjs,check-test-report.mjs,contracts.json,failures/*.json,prerequisites.mjs}`, package test scripts, `.ci-results` upload | Required tests cannot disappear, pass empty, skip, or conceal abnormal exit; CLI build and CDK stamping occur before their tests. |
| Source/config policy | `scripts/ci/{check-test-policy.mjs,test-configuration.mjs}`, `scripts/tests/ci-test-policy.test.mjs`, every `*.test.*`, `*.stories.*`, Vitest/Vite/Playwright config, local config dependencies, and the two CI runner modules | 433 source/config/runner files scanned. Searched direct/chained/aliased/destructured/namespace test APIs, options, retries, selections, expected failures, module mocks, mutable/escaped objects, tuple data, custom configs, and runner argv. |
| PostgreSQL and migrations | `.github/workflows/ci.yaml` test/migrate jobs; backend DB client/schema/migration entry points; RLS and sandbox-persistence contracts | Real migrated PostgreSQL runs under the application role; fresh/previous migration job remains a distinct CI owner. |
| Git, revision, hydration, projections | `apps/backend/src/services/git`, workspace `revision`, `hydrate`, `derived-stores`, OpenWorkflow hydrate/index/projection paths, hydration contract | Gate 1 proves real current execution and cleanup. Duplicate revision/projection semantics remain visible and assigned to Gate 2. |
| Write jobs and push | workspace `write-*`, `git-file-jobs`, OpenWorkflow enqueue/workflow files, conversation publish, bare-remote/ref assertions | All 12 kinds execute as characterization and every remote ref is checked unchanged. Successful transactional write ownership remains assigned to Gate 3. |
| Chat/sandbox lifecycle | workspace `tanstack-*`, `workspace-chat-*`, `chat-{lifecycle,runtime}`, `sandbox-{registry,provider,instance-store}`, conversation HTTP/WebSocket routes | Real two-turn persistence/reconstruction/destruction is covered. Competing lifecycle owners were searched and intentionally remain for Gate 4 deletion. |
| UI entry points | Home pending compose, `WorkspaceChat*`, `WorkspacePane`, file tree/Pierre file, WebSocket transport, publish/query/mutation helpers, stories/tests | Gate 1 runs the complete current UI suite and builds the production image. UI choreography/polling/ownership is intentionally deferred to Gate 5. |
| Codesearch | codesearch package scripts, Dockerfiles, route/warmup tests, runtime/start script, Node/Bun split runner, report oracle | Full discovered inventory executes: Node cases plus Bun.Glob cases; production toolchain/image entry point is built. |
| Packages/deployment/docs | CLI and CDK packages, self-host example, Terraform, docs/UI/backend/worker/codesearch/OTEL Dockerfiles, `.dockerignore` | Distributables, consumer typecheck, Terraform, and every production image have explicit jobs. Node build stages and Bun service runtimes match ADR-031/ADR-002. |
| Repository-wide old vocabulary/parallel paths | `rg` searches for `skip`, `fails`, `todo`, `retry`, `vi.mock`, `mock.module`, revision/generation/projection fields, write runners, registries, polling/invalidation, compose/pending IDs | No Gate 1 deletion omission found. Remaining product owners map to Gates 2–6 below rather than silently disappearing. |

## Surviving owners and explicit later-gate disposition

These are not Gate 1 blockers because Gate 1 is restricted to truthful CI and must not fix product behavior.

| Later gate / owner | Surviving code/state | Required disposition |
| --- | --- | --- |
| **Gate 2 — revision/projection** | `db/schema/workspaces.ts`; `models/workspaces.ts`; workspace `revision.ts`, `hydrate.ts`, `derived-stores.ts`; OpenWorkflow hydrate/index/commit-projection; desired/indexed/active SHA and generation compatibility fields; provider-specific reconstruction callers | Introduce the single `WorkspaceRevision`/`ProjectionState`, route all readers/writers through it, prove CAS/races/failure/no-op, and delete redundant/provider-specific reconstruction. |
| **Gate 3 — transactional writes** | `write-job-intent.ts`, `write-jobs.ts`, `write-runner.ts`, `git-file-jobs.ts`, `job-worktree.ts`, `openworkflow/{enqueue-workspace-write-commit,workflows/workspace-write-commit}.ts`, conversation publish paths; `write-workflow.contract.test.ts` exact-failure characterization | Establish one typed OpenWorkflow/native-git stage/validate/commit/CAS/push owner, credential boundary and recovery semantics; delete alternative runners/choreography and replace all 12 failure characterizations with successful proof. |
| **Gate 4 — conversation/sandbox** | `sandbox-registry.ts`, `chat-lifecycle.ts`, `chat-runtime.ts`, `workspace-chat-send-runtime.ts`, `tanstack-workspace-chat.ts`, persistence/AG-UI/transport repair, conversation HTTP/WebSocket routes; mocked `tanstack-workspace-chat.live.test.ts` characterization | Make TanStack `defineSandbox` plus Postgres instance store authoritative, unify HTTP/WebSocket/prepare, remove registries/manual repair/catch-empty/per-turn provider work, and add restart/two-replica/resume/race proof. |
| **Gate 5 — UI workflow** | `pending-workspace-compose.ts`, `WorkspaceChatSession.tsx`, `WorkspacePane.tsx`, `workspaceChatWebSocket.ts`, `conversationPublish.ts`, query/invalidation/polling and file-tree mutation helpers, current simulated stories/tests | Replace draft choreography with one server command, keep one `useChat`/transport owner, canonicalize IDs, give tree/publish/save state one owner, replace polling waterfalls, and add required Storybook browser interactions. |
| **Gate 6 — cleanup/golden journey** | `scripts/ci/diagnostics/*.json`, `scripts/ci/failures/*.json`, Gate 0/1 characterizations, recovery-only policy/evidence scaffolding, compatibility mappings/state columns, parallel fixtures/mocks | Run mandatory deterministic Playwright and all affected final gates; delete characterizations after stronger proof, stale allowances/adapters/state and redundant fixtures, then record positive deletion and final measurements. |

No old owner that Gate 1 itself required to remove remains: path-filter typecheck behavior, database/RLS exclusions, OpenCode opt-in skip, Bun.Glob skips, UI empty-suite success, and the CLI empty live-eval placeholder were removed or made fail-closed.

## Commands and searches used

- Commit/diff scope: `git rev-parse`, `git log --oneline`, `git show --stat`, `git diff 7dfa6b93...d8785835`, `git diff --name-only 7dfa6b93...d8785835`, `git status --short`, and `git ls-remote origin refs/heads/codex/develop-plan-to-refocus-branch-direction`.
- Standards/spec reads: `nl -ba`/`sed`/`cat` over the recovery plan, Gate 0/Gate 1 baselines, root and app `AGENTS.md`, TDD/testing/mocking/code-review skills, ADR-002/028/031, workflows, scripts, Dockerfiles, package manifests, contract tests, and retained logs/JSON.
- Repository coverage: `rg --files` plus `rg -n` searches for workflow/package/script entry points; test/config files; `skip|fails|todo|only|retry`; `vi.mock|mock.module|spyOn`; revision/generation/projection; hydrate/index; write/commit/push; chat/sandbox registries/transports; UI compose/pending/poll/invalidate/publish/file-tree owners.
- Executed verification at the final candidate: `volta run --node 22.16.0 node --test scripts/tests/ci-*.test.mjs` (5/5 pass) and `volta run --node 22.16.0 pnpm test:policy` (433 source/config/runner plus 27 command files pass).
- Terminal CI verification: parsed `ci-d8785835-complete.json` with `jq` (exact SHA, `completed/success`, 13/13 jobs successful), then independently queried GitHub's run and paginated jobs APIs with `gh api repos/ctxpipe-ai/ctxpipe/actions/runs/34174216707{,/jobs}`; both returned the same SHA and 13 successful jobs.
- Independent temporary-fixture probes across the review sequence: chained/aliased/destructured/namespace selectors and mocks; Playwright fail/fixme; retry shorthand, lexical shadowing, computed/template keys, getters, mutable/reassigned/aliased/property/element/increment/delete options; `Object.assign/freeze/seal`; tuple spreads and table data; YAML folded commands and shell continuations; custom ESM/re-export/CJS config graphs; pnpm filtered/`-C`, npm workspace and Turbo forwarding; static/dynamic runner argv; explicit zero/false controls. Failed probes were re-run after each correction.

## Terminal condition

**PASS — Gate 1 is complete with zero standards blockers.** The reviewed and pushed terminal checkpoint is `d87858354a783a9fd95c46785208c9b699a45e3b`. The remote branch resolves to that exact SHA, and GitHub Actions run `34174216707` completed successfully at that SHA with all 13 jobs green. Gates 2–6 retain the product owners and acceptance work listed above.
