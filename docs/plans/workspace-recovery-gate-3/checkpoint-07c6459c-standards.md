# Gate 3 eleven-kind checkpoint — Standards review

Pinned range: `bb24210c01e0dc1bcbc2d4fdf197d07a7a17035d...07c6459cc260a5be65183ab3cc1d485815d0fb5b`

1. **[P1, documented transaction/binding breach] Connector ownership is checked from two independently committed snapshots.** `connector-mirror.ts:31-32` first calls a provider binding reader and then `getRepositoryReadBinding`; every reader opens and commits its own `withOrgDbContext` transaction. If a connection is rebound from repository A after line 31, repository A can remain valid at line 32, so the old binding and A’s current repository row satisfy every predicate. This can occur in the “immediately after credential issuance” check at `write-broker.ts:97-99`, allowing a stale provider command to push after detachment. The reset proof changes the binding before the whole assertion and does not cover this interleaving. Backend `AGENTS.md:11` requires multi-table operations in one transaction, while ADR-033:17 requires full binding identity immediately before push. Return `gitUrl` with the repository already joined by each provider reader, or add one model query that validates the complete tuple in one SQL statement; add a barrier-based native race proof.

2. **[Judgement call: possible Mysterious Name] Normal extraction is expressed through migration-only names.** `workspace-extract-ingest.ts:92-128` names its durable step `load-legacy-source` and calls `loadMigrationExportSource` plus `planMigrationExport`. Those now serve both ongoing extraction and one-time migration, so their names conceal their broader behavior. Rename the shared source/planner around knowledge projection and leave migration-specific cutover policy in each workflow.

**Result:** one blocking documented P1 and one non-blocking Fowler heuristic. The alias-comment finding and the five other nine-kind findings are corrected in the pinned blobs. The repetitive workflow step sequence is required by ADR-033 and is suppressed as a duplication smell. The acknowledged command-property bag and declared unfinished work are excluded.
