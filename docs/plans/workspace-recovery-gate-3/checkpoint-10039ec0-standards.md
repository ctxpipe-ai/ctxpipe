# Gate 3 nine-kind checkpoint — Standards review

Pinned range: `bb24210c01e0dc1bcbc2d4fdf197d07a7a17035d...10039ec00185b12c30aba616f6624a0abbd520f0`

1. **[P2, documented behavior breach; possible Duplicated Code] Rename repair drops comments on aliased claims.** In `native-rename-rewrite.ts:208-217`, an aliased claim item is detached with `document.createNode(claim.toJS(document))`, but its `comment` and `commentBefore` are not copied. Thus a valid item such as `- *claim # owner note` loses the owner note whenever its `to` is rewritten. This contradicts the checkpoint’s promise that YAML document updates preserve comments (`status.md:63`) and the rename transform preserves unknown metadata (`status.md:147`), product behavior ADR-033:19 requires the native migration to retain. The same detach shape already preserves both fields in `hydrate-write-jobs.ts:172-180`, `migration-export.ts:137-143`, and `knowledge-metadata.ts:37-45`; centralize that operation and use it here. A focused YAML-library probe confirmed `createNode(alias.toJS(...))` discards the inline comment unless it is copied explicitly. Add a native rename case with an aliased claim and comments.

**Result:** one blocking P2 documented behavior finding, also labelled as one Fowler Duplicated Code judgement call. The six prior checkpoint defects are corrected in the pinned blobs. No additional documented-standard breach or smell crossed the reporting threshold. ADR-033 expressly requires the otherwise repetitive per-workflow durable step sequence, so it is not reported as duplication. The acknowledged command-property-bag heuristic and declared unfinished Gate 3 work remain outside this checkpoint finding count.
