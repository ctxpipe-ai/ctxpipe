# Gate 3 five-kind checkpoint — Standards review

Pinned range: `bb24210c01e0dc1bcbc2d4fdf197d07a7a17035d...f7119635da09e39f853e9b6c64770924538a3006`

1. **[P1, documented behavior breach] Import-key cleanup can corrupt valid YAML or falsely report completion.** `apps/backend/src/domain/workspaces/hydrate-write-jobs.ts:189-209` first accepts any non-null parsed `import_key`, then deletes only the physical key line with a regex. A valid block scalar such as `import_key: >-\n  legacy:key` leaves the indented scalar body behind as malformed metadata. A BOM-prefixed front matter document is parsed as containing the key but fails the subsequent `^---` match, so the workflow completes as a no-op while the key remains. The reconstruction also changes CRLF delimiters while promising to remove only the intended key. This violates ADR-033:19's requirement to preserve product behavior and the checkpoint's stated “removes only the intended knowledge import key” behavior (`status.md:63`). Delete the complete YAML node through the document parser (and preserve the original body/delimiters), then add literal native proof for block-scalar and BOM/CRLF inputs.

2. **[Judgement call — Mysterious Name / Divergent Change]** `apps/backend/src/models/workspace-write-jobs.ts:298-323` names a mutating status-reconciliation transaction `getWorkspaceWriteJob`. Polling this apparent query updates durable state and its timestamp before selecting. Split `reconcileTerminalWorkspaceWriteJob` from the read or rename the operation so callers can see that it writes; keep both statements in the existing short org transaction.

3. **[Judgement call — Duplicated Code]** The Markdown discovery/read loop is copied in `workspace-import-key-cleanup.ts:89-128`, `workspace-claims-upgrade.ts:90-135`, and `workspace-valid-from-persist.ts:113-167`, including one Git subprocess per file. Extract an ordinary native-Git helper that returns the immutable filtered file set. Keep each workflow's durable `step.run` sequence explicit as ADR-033:11 requires.

**Result:** one blocking documented-standard/correctness finding and two refactoring smells. Publication-before-completion, canonical hydrate enqueue, file-mode preservation, credential ownership, and short SQL boundaries are corrected or conformant in this pinned checkpoint.
