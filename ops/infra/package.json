{
  "name": "infra",
  "main": "index.ts",
  "scripts": {
    "build": "pnpm run build:railway && tsc",
    "build:railway": "pnpm --dir sdks/railway run build"
  },
  "devDependencies": {
    "@types/node": "^18",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@pulumi/neon": "file:sdks/neon",
    "@pulumi/pulumi": "^3.113.0",
    "@pulumi/railway": "file:sdks/railway"
  }
}
