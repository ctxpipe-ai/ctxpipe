job_name: benchmark-baseline-cursor
jobs_dir: jobs/benchmarks
n_concurrent_trials: 1
quiet: false
agents:
  - name: cursor-cli
    model_name: cursor/composer-2.5
    env:
      CURSOR_API_KEY: ${CURSOR_API_KEY}
tasks:
  - path: benchmarks/tasks/ctxpipe/boxyhq-env-bridge
