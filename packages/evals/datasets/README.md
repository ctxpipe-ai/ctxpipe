# Datasets

Add shared test fragments here as the benchmark grows (e.g. `smoke.yaml` imported from `promptfooconfig.yaml` via `tests: file://./datasets/smoke.yaml`).

The default smoke cases live in `../promptfooconfig.yaml`.
