# Datasets

Add shared test fragments here as the benchmark grows (e.g. `smoke.yaml` imported from `promptfooconfig.yaml` via `tests: file://./datasets/smoke.yaml`); retrieval cases live in `benchmark.yaml` and expect [better-auth](https://github.com/better-auth) indexed in the org.

The default smoke cases live in `../promptfooconfig.yaml`.
