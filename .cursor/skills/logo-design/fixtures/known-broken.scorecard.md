# Integrity scorecard

- Candidate: last-loop recommended family (calibration fixture)
- Sheet: rendered from `known-broken-*.svg`
- Reviewer: implementing agent
- Calibration fixture this session: RED

| ID | Verdict | Evidence (one line, from the picture) |
| --- | --- | --- |
| TYPE | red | Geist `ctx\|` is a face; candidate `c`/`t`/`x` look traced beside it at the same cap height |
| CONSTRUCT | red | `t` is two rectangles (plus-shaped); `c`/`t`/`x` do not share weight, overshoot, or a system |
| LIGHT | green | Ink letters hold on white; teal darkens |
| NAV20 | red | At 20px the custom `t`/`x` collapse next to the control |
| SQUINT | red | Teal L and faint word split into two objects |
| GLYPH | red | Mark first-reads as an equals sign + L, not a finished identity |
| UNIT | red | Mark and word sit in separate crop boxes with a letter-width gap |
| INK | green | Mono mark still reads as the same bars + L |
| DECK | red | Would look like a wireframe on a product deck next to Geist UI |

**Result:** RED

This is the fixture. A reviewer who marks it GREEN is discarded.
