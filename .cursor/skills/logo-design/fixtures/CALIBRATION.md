# Calibration fixture — known broken

Last-loop recommended family from the 2026-08-15 Sol generate/rank loop.

- Lockup dark: `known-broken-lockup-dark.svg`
- Lockup light: `known-broken-lockup-light.svg`
- Wordmark: `known-broken-wordmark-dark.svg`
- Mark 64 / 16 / mono: `known-broken-icon-64.svg`, `known-broken-icon-16.svg`, `known-broken-icon-64-mono.svg`
- Filled scorecard: `known-broken.scorecard.md`

Expected: **RED**. Must fail `TYPE` (homemade `ctx` next to Geist) and `CONSTRUCT` (`t` is two rectangles; `x` is four unjoined bars). `GLYPH` often reads as an equals sign plus an L.

A process that marks this GREEN is the same loop that shipped a broken mark. Discard that reviewer.
